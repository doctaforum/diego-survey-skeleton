<div class="d-flex justify-content-between align-items-end my-3">
    <p class="questionItem"><i>{{ questionItem }}</i></p>
    <div class="d-flex justify-content-between align-items-center">
        <div class="radio-column d-flex flex-column justify-content-center align-items-center">
            <label for="{{ question }}">1</label>
            <input type="radio" name="{{ question }}" value="1" class="form-check-input" required='{{ isRequired }}'>
        </div>
        <div class="radio-column d-flex flex-column justify-content-center align-items-center">
            <label for="{{ question }}">2</label>
            <input type="radio" name="{{ question }}" value="2" class="form-check-input" required='{{ isRequired }}'>
        </div>
        <div class="radio-column d-flex flex-column justify-content-center align-items-center">
            <label for="{{ question }}">3</label>
            <input type="radio" name="{{ question }}" value="3" class="form-check-input" required='{{ isRequired }}'>
        </div>
        <div class="radio-column d-flex flex-column justify-content-center align-items-center">
            <label for="{{ question }}">4</label>
            <input type="radio" name="{{ question }}" value="4" class="form-check-input" required='{{ isRequired }}'>
        </div>
        <div class="radio-column d-flex flex-column justify-content-center align-items-center">
            <label for="{{ question }}">5</label>
            <input type="radio" name="{{ question }}" value="5" class="form-check-input" required='{{ isRequired }}'>
        </div>
    </div>
</div>